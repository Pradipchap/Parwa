import 'package:flutter/material.dart';


class Pujari extends StatelessWidget {
  const Pujari({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      backgroundColor: Colors.white,
      appBar: 
      AppBar(title: const Text('pujari'),),
    )
    ;
    
  }
}